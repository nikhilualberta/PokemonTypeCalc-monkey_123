import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.poketype',
  appName: 'PokeTypeCalc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
